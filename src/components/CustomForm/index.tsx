// React
import React from 'react';

// Custom

interface CustomFormProps {
  canActivate?: boolean;
};

export const CustomForm: React.FC<CustomFormProps> = ({
  canActivate
}) => {
  return (
    <></>
  );
};
